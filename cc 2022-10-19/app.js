// Write a function thatt when given a URL as a String, parses out just the domain name and returns it as a string. For Example: 
// domainName('http://github.com/carbonfive/raygun') == 'github'
// domainName('http://www.zombie-bites.com') == 'zombie-bites'
// domainName('http://www.cnet.com') == 'cnet'


//Prep Parameters Return Example Psuedo
//Parameter is a URL string
//return just the domain name as a string
//example above


function parseDomain(){
    
}