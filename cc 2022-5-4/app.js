//Simple, remove the spaces from the string, then return the resultant string.

function noSpace(x){
    return x.split(' ').join('') //using split(),remember to keep the extra space for the delimiter or wont work
  }
  
